/*
Directions:
Given a linked list, return true if the list is circular, false if it is not.
eg:

const l = new List();
const a = new Node("a")
const b = new Node("b")
const c = new Node("c")

*/

function circular(list) {
	let slow = list.head;
	let fast = list.head;
}
